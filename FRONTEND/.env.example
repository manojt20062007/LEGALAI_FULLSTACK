# LM-Verify API Configuration
# Base URL for the FastAPI backend (Default: http://localhost:8000)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Optional: Set to 'true' to use simulated mock mode for testing without backend running
# Set to 'false' (default) for production / live FastAPI communication
NEXT_PUBLIC_USE_MOCK_API=false
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME="r57rephm"
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET="legalai_preset"
